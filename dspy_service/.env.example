# ========================================
# DSPy AI Service Configuration Template
# ========================================
#
# Instructions:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Fill in your OpenAI API key
# 3. Adjust other settings as needed
# 4. Restart the DSPy service

# ========================================
# LLM Configuration (Required)
# ========================================

# Get your API key from: https://platform.openai.com/api-keys
# IMPORTANT: Replace this with your actual OpenAI API key
OPENAI_API_KEY=your_openai_api_key_here

# LLM Provider (default: openai)
# Options: openai, anthropic, cohere, etc.
LLM_PROVIDER=openai

# OpenAI Model to use (default: gpt-4o-mini)
# Recommended models:
#   - gpt-4o-mini: Fast and cost-effective (~$0.15/$0.60 per 1M tokens)
#   - gpt-4o: More capable but expensive (~$2.50/$10 per 1M tokens)
#   - gpt-3.5-turbo: Cheapest option (~$0.50/$1.50 per 1M tokens)
OPENAI_MODEL=gpt-4o-mini

# ========================================
# Embedding Model Configuration
# ========================================

# Embedding model for RAG/search (default: text-embedding-3-small)
# Options:
#   - text-embedding-3-small: Balanced performance (~$0.02 per 1M tokens)
#   - text-embedding-3-large: Higher quality (~$0.13 per 1M tokens)
#   - text-embedding-ada-002: Legacy model
EMBEDDING_MODEL=text-embedding-3-small

# ========================================
# Database Configuration
# ========================================

# PostgreSQL database settings
DB_NAME=postgres
DB_USER=postgres
DB_PASS=123456
DB_HOST=localhost
DB_PORT=5432

# ========================================
# Server Configuration
# ========================================

# Flask environment (development or production)
FLASK_ENV=development

# Port for DSPy service
PORT=8000

# ========================================
# Notes
# ========================================
#
# Cost Estimates (approximate):
# - Q&A per query: ~$0.001-0.01 depending on model
# - Search with embeddings: ~$0.0001 per search
# - Review summarization: ~$0.002-0.01 per summary
#
# Set usage limits on OpenAI dashboard: https://platform.openai.com/settings/organization/billing
# Recommended limit for testing: $5-10/month
#
